# AGENTS.md instructions for /Users/raccoon/Documents/music

## 工作偏好

- 默认使用中文沟通，除非用户指定其他语言。
- 修改前先检查当前项目的现有实现、约定和工具链。
- 优先给出实际改动、关键路径和验证结果；无法验证时说明原因。

## 开发文档

- 将 `README.md` 作为项目开发文档维护。
- 涉及启动方式、项目结构、功能行为、使用流程、验证方式或重要开发决策的改动时，同步更新 `README.md`。
- 不要把过期的运行命令、废弃工具说明或与当前实现不符的开发记录留在 `README.md` 中。

## 版本控制

- 在非 git 目录中工作时，如果该目录适合作为 git 项目管理，先暂停改动并询问是否初始化仓库和生成 `.gitignore`。
- 不擅自初始化仓库；如确认新建仓库，先确认跟踪范围和忽略规则。
- 如果用户确认该项目不纳入 git 管理，在项目级 `AGENTS.md` 记录该决定和后续无需创建 commit 的规则。
- 在 git 仓库完成代码或文档变更后创建 commit；提交前只暂存本次任务相关文件。
- commit message 使用中文约定式提交。极小改动可一行；非极小改动使用正文说明改动内容、原因、影响范围和验证方式。

## 代码约定

- 遵循项目已有风格。新增或修改函数时补充清晰的中文注释或文档，除非项目已有更具体的语言约定。
- 项目内 Python 工作优先使用虚拟环境；一次性临时脚本可例外。
- 设计数据库结构时，必须添加中文字段注释。

## 工具与流程

- 仅在用户明确要求或任务确实适合拆分时创建 subagent；创建前说明模型和思考深度选择。
- 涉及带箭头的 SVG 流程图、有向关系图或架构图时，使用 `svg-diagram-qa` 校验连线和箭头；普通图标和装饰性 SVG 不需要。
